---
import PageLayout from '../playground/layouts/PageLayout.astro';
import * as post from '../data/index.mdx';

const metadata = {
  title: 'Astro UI — Free template for creating websites with Astro + Tailwind CSS',
  ignoreTitleTemplate: true,
};
---

<PageLayout metadata={metadata} title="Index">
  {post.Content && <post.Content />}
</PageLayout>
