---
//import type { SiteVerificationProps as Props } from './types';
import { SITE } from 'vendor:config';
---

{SITE.googleSiteVerificationId && <meta name="google-site-verification" content={SITE.googleSiteVerificationId} />}
