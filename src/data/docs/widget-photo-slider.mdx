---
title: Carousel - Photo Slider
category: Widget
---

import Demo from '~/playground/components/Demo.astro';
import WidgetSwiperPhotoSlider from '~/components/WidgetSwiperPhotoSlider/WidgetSwiperPhotoSlider.astro';

# Carousel - Photo Slider


<Demo title="">
  <WidgetSwiperPhotoSlider
    title="More ideas and inspiration"
    subtitle="Embark on a design journey that surpasses pixels, entering a realm of imagination."
    imagesFolder="portfolio_01"
  />
</Demo>

### Usage

1. Import photoswipe styles into root layout (Page Layout.astro components)

```astro
---
import "photoswipe/style.css";
---
```

2. Add to astro config file

```ts
import { defineConfig } from 'astro/config';

export default defineConfig({
  vite: {
    ssr: {
      noExternal: ['photoswipe'],
    }
  }
})
```

```astro
---
import { WidgetSwiperPhotoSlider } from '@yatoday/astro-ui/astro';
---

<!-- All images in the directory /src/assets/images/folder_01 will be added to the gallery -->
<WidgetSwiperPhotoSlider imagesFolder="folder_01" />
```

## Props

<div className="overflow-x-auto">
  | Prop | Type | Default | Description |
  |------------|------------|------------|------------|
  | `imagesFolder` | `string` | - | Path to the folder with images relative to /src/assets/images/`{imagesFolder}` |
  | `id` | `string` | js random | By default, the `id` is generated automatically. If you decide to specify this parameter yourself, make sure that it is unique on the page |
  | `title` | `string` | - | `Title` prop in [Headline](/docs/headline) component |
  | `subtitle` | `string` | - | `Subtitle` prop in [Headline](/docs/headline) component |
  | `tagline` | `string` | - | `Tagline` prop in [Headline](/docs/headline) component |
  | `isAfterContent` | `boolean` | `false` | - |
  | `isDark` | `boolean` | `false` | Adds the `dark` CSS class to the [WidgetWrapper](/docs/feature-widget-wrapper) |
  | `bg` | `string` | - | Set background element |
  | `isAfterContent` | `boolean` | `false` | - |
  | `withNavigation` | `boolean` | `true` | Navigation visibility |
  | `classes.container` | `string` | - | CSS classes for the [WidgetWrapper](/docs/feature-widget-wrapper) container |
  | `classes.loading` | `string` | - | CSS classes for loading layout |
  | `classes.headline.{...}` | `string` | - | CSS classes for the [Headline](/docs/headline) component |
  | `autoplay` | `boolean` | `false` | `Object` with autoplay parameters or `boolean` true to enable with default settings |
  | `...rest` | `any` | - | ...rest params from Swiper js [parameters](https://swiperjs.com/swiper-api#parameters) |
</div>
